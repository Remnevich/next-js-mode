import {Button} from "@/components/ui/button";

export default async function Home() {
    return (
        <div>
            <Button variant={'default'} size={'sm'}>Click here</Button>
        </div>
    );
}
